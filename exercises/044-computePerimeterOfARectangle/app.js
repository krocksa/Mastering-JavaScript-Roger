function computePerimeterOfARectangle(length, width) {
  // your code here
  let perimetro = (2*length) + (2*width)
  return perimetro
}
let output = computePerimeterOfARectangle(5, 2);
console.log(output);